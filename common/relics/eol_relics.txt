@activation_cost = 150
@triumph_duration = 3600
@triumph_duration_short = 1800
@triumph_duration_veryshort = 900

r_custodian_beacon = {
	activation_duration = @triumph_duration
	portrait = "GFX_relic_custodian_beacon"
	sound = "relic_activation_the_surveyor"

	resources = {
		category = relics
		# Activation cost
		cost = {
			influence = 250
		}
	}

	ai_weight = {
		weight = 100
	}

	triggered_country_modifier = {
		potential = {
			always = yes
		}
		pop_ethic_materialist_attraction_mult = 0.20
		pop_ethic_xenophile_attraction_mult = 0.20
	}

	score = 100

	active_effect = {
		custom_tooltip = relic_summon_custodians
		custom_tooltip = relic_triumph_cooldown
		hidden_effect = {
			add_modifier = {
				modifier = "relic_activation_cooldown"
				days = @triumph_duration
			}
			country_event = { id = eolrelics.1 }
		}
	}

	# Possible check for activation
	possible = {
		custom_tooltip = {
			fail_text = "requires_relic_no_cooldown"
			NOT = { has_modifier = relic_activation_cooldown }
		}
	}
}
